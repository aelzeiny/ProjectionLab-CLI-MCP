[project]
name = "projectionlab-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.25,<2",
    "playwright>=1.40",
    "python-dotenv>=1.0",
]

[project.scripts]
projectionlab-mcp = "projectionlab_mcp.server:main"
projectionlab = "projectionlab_mcp.cli:main"
pl = "projectionlab_mcp.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
