PROJECTIONLAB_EMAIL=your@email.com
PROJECTIONLAB_PASSWORD=yourpassword
PROJECTIONLAB_API_KEY=

# Dev mode: share browser with browser-mcp via CDP (leave unset in production)
# CDP_PORT=9222

# Set to "false" for a visible browser in production mode (ignored in dev mode)
# HEADLESS=true
